'use client';
import {useEffect,useMemo,useState} from 'react';
import Link from 'next/link';
import SiteNav from '@/app/components/SiteNav';
const whatsappNumber='201515227612';
const suggestedQuestions=[
  'الكورس ده مناسب للمبتدئين؟',
  'هتعلم إيه من الكورس ده؟',
  'What will I learn from this course?',
  'How long is this course?',
  'What are the prerequisites?',
  'ممكن تشرحلي ترتيب الدروس؟'
];
export default function CoursePage({params}:{params:Promise<{slug:string}>}){const [slug,setSlug]=useState('');const [data,setData]=useState<any>(null);const [q,setQ]=useState('');const [answer,setAnswer]=useState('');const [busy,setBusy]=useState(false);useEffect(()=>{params.then(p=>{setSlug(p.slug);fetch(`/api/courses/${p.slug}`).then(r=>r.json()).then(setData)})},[params]);const purchaseUrl=useMemo(()=>{if(!data?.course||data.course.is_free)return '';const c=data.course;const message=`السلام عليكم، أريد شراء كورس ${c.title}.\\nالسعر الحالي: ${c.price} EGP\\nUsername/Email: `;return `https://wa.me/${whatsappNumber}?text=${encodeURIComponent(message)}`},[data]);const ask=async(message?:string)=>{const question=(message??q).trim();if(!question||busy)return;setQ(question);setBusy(true);setAnswer('');try{const r=await fetch('/api/ai/course',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({slug,question})});const j=await r.json();setAnswer(j.answer||j.error||'No answer.')}catch{setAnswer('AI assistant is temporarily unavailable. Please try again.')}finally{setBusy(false)}};if(!data)return <main><div className="shell"><SiteNav/><div className="section"><p className="muted">Loading playlist…</p></div></div></main>;if(data.error)return <main><div className="shell"><SiteNav/><div className="section"><div className="error">{data.error}</div><Link className="btn" href="/courses">Back to academy</Link></div></div></main>;const c=data.course;return <main><div className="shell"><SiteNav/><section className="coursehero"><span className="tag">{c.category}</span><h1>{c.title}</h1><p className="muted" style={{maxWidth:800,lineHeight:1.8}}>{c.description}</p><div className="chips"><span className="chip">{c.level}</span><span className="chip">{Math.round(c.duration_minutes/60*10)/10} hours</span><span className="chip">{c.is_free?'Free':`${c.price} EGP`}</span><span className="chip">{data.enrolled?'Access granted':'Access pending'}</span></div>{!c.is_free&&!data.enrolled&&<div className="actions"><a className="btn primary" href={purchaseUrl} target="_blank" rel="noreferrer">Buy this course on WhatsApp</a></div>}{c.is_free&&!data.enrolled&&<div className="actions"><Link className="btn primary" href="/register">Join for free</Link><Link className="btn" href="/login">Sign in</Link></div>}</section><section className="section" style={{paddingTop:25}}>{data.enrolled?<><h2>Playlist</h2><div>{data.lessons.map((l:any)=><div className="lesson" key={l.id}><div style={{flex:1}}><b>{l.position}. {l.title}</b><div className="small muted">{l.duration_minutes} minutes {l.file_name?'· '+l.file_name:''}</div>{l.video_url&&<video controls preload="metadata" style={{width:'100%',marginTop:14,borderRadius:12}} src={l.video_url}/>} {l.file_url&&<div className="actions"><a className="btn" href={l.file_url} target="_blank" rel="noreferrer">Open lesson file</a></div>}</div><span className="tag">Protected</span></div>)}</div><div className="ai"><div className="eyebrow">Course AI</div><h3>Ask me anything.</h3><p className="muted">Arabic, Egyptian Arabic, or English — course questions and everyday conversation are supported.</p><div style={{display:'flex',flexWrap:'wrap',gap:8,margin:'16px 0'}}>{suggestedQuestions.map((question)=><button key={question} className="iconBtn" onClick={()=>ask(question)} disabled={busy}>{question}</button>)}</div><div className="field"><textarea value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();ask()}}} placeholder="اسأل بالعربي أو English…"/></div><button className="btn primary" onClick={()=>ask()} disabled={busy}>{busy?'Thinking…':'Ask AI'}</button>{answer&&<p style={{lineHeight:1.8,whiteSpace:'pre-wrap',marginTop:16}}>{answer}</p>}</div></>:<div className="card"><h3>{c.is_free?'Free playlist — sign in to start.':'Ready to buy? Contact me on WhatsApp.'}</h3><p className="muted">{c.is_free?'Create an account or sign in and the lessons will become available.':'The current price is shown above. Tap the WhatsApp button to send a ready-made purchase message. Payment is arranged with the instructor directly.'}</p><div className="actions">{c.is_free?<><Link className="btn primary" href="/register">Register</Link><Link className="btn" href="/login">Sign in</Link></>:<a className="btn primary" href={purchaseUrl} target="_blank" rel="noreferrer">Buy this course</a>}</div></div>}</section></div></main>}