'use client';
import {useState} from 'react';
import Link from 'next/link';
export default function AdminSetup(){const [secret,setSecret]=useState('');const [password,setPassword]=useState('');const [show,setShow]=useState(false);const [message,setMessage]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const submit=async()=>{setBusy(true);setError('');setMessage('');const res=await fetch('/api/auth/bootstrap',{method:'POST',headers:{'Content-Type':'application/json','x-admin-bootstrap-secret':secret},body:JSON.stringify({password})});const data=await res.json();setBusy(false);if(!res.ok){setError(data.error||'Setup failed.');return}setMessage('Administrator account created. You can now sign in with the administrator email and the temporary password you chose. Change it immediately from My account.');};return <main className="formwrap"><div className="form"><Link className="brand" href="/">MARWAN.SWEDAN</Link><h1>Administrator setup.</h1><p className="muted">One-time protected setup for the first administrator account.</p>{message&&<div className="success">{message}</div>}{error&&<div className="error">{error}</div>}<div className="field"><label>Bootstrap secret</label><input type="password" value={secret} onChange={e=>setSecret(e.target.value)} autoComplete="off"/></div><div className="field"><label>Temporary password</label><div style={{display:'flex',gap:8}}><input style={{flex:1}} type={show?'text':'password'} minLength={8} value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn" type="button" onClick={()=>setShow(!show)}>{show?'Hide':'Show'}</button></div><span className="small muted">Use a temporary password you can change immediately after signing in.</span></div><button className="btn primary" style={{width:'100%'}} disabled={busy||!secret||!password} onClick={submit}>{busy?'Setting up…':'Create administrator'}</button><p className="small muted">The setup endpoint refuses to run if an administrator already exists.</p><Link className="small muted" href="/login">Go to sign in</Link></div></main>}
