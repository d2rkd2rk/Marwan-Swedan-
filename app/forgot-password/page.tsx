'use client';
import {FormEvent,useState} from 'react';
import Link from 'next/link';
export default function ForgotPassword(){const [identifier,setIdentifier]=useState('');const [challengeId,setChallengeId]=useState('');const [code,setCode]=useState('');const [password,setPassword]=useState('');const [show,setShow]=useState(false);const [message,setMessage]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const requestCode=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');const r=await fetch('/api/auth/forgot-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({identifier})});const j=await r.json();setBusy(false);if(!r.ok){setError(j.error||'Unable to send code.');return}if(j.challengeId){setChallengeId(j.challengeId);setMessage('A verification code was sent to the email on the account.')}else setMessage('If the account exists, a verification code was sent to its email.');};const reset=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');const r=await fetch('/api/auth/reset-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({challengeId,code,newPassword:password})});const j=await r.json();setBusy(false);if(!r.ok){setError(j.error||'Password reset failed.');return}location.href='/login'};return <main className="formwrap"><div className="form"><Link className="brand" href="/">MARWAN.SWEDAN</Link><h1>Reset your password.</h1>{error&&<div className="error">{error}</div>}{message&&<div className="success">{message}</div>}{!challengeId?<form onSubmit={requestCode}><div className="field"><label>Username or email</label><input required value={identifier} onChange={e=>setIdentifier(e.target.value)}/></div><button className="btn primary" style={{width:'100%'}} disabled={busy}>{busy?'Sending…':'Send verification code'}</button></form>:<form onSubmit={reset}><div className="field"><label>Verification code</label><input required inputMode="numeric" pattern="[0-9]{6}" maxLength={6} autoComplete="one-time-code" value={code} onChange={e=>setCode(e.target.value)}/></div><div className="field"><label>New password</label><div style={{display:'flex',gap:8}}><input style={{flex:1}} required minLength={8} type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn" type="button" onClick={()=>setShow(!show)}>{show?'Hide':'Show'}</button></div><span className="small muted">8+ characters, at least one number and one special character.</span></div><button className="btn primary" style={{width:'100%'}} disabled={busy}>{busy?'Saving…':'Set new password'}</button></form>}<p className="small muted"><Link href="/login">Back to sign in</Link></p></div></main>}