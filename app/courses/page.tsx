'use client';
import {useEffect,useState} from 'react';
import Link from 'next/link';
export default function Courses(){const [courses,setCourses]=useState<any[]>([]);const [loading,setLoading]=useState(true);useEffect(()=>{fetch('/api/courses').then(r=>r.json()).then(x=>setCourses(x.courses||[])).finally(()=>setLoading(false))},[]);return <main><div className="shell"><nav className="nav"><Link className="brand" href="/">MARWAN.SWEDAN</Link><div className="navlinks"><Link href="/">Portfolio</Link><Link href="/courses">Courses</Link></div><div className="actions" style={{margin:0}}><Link className="btn" href="/login">Sign in</Link><Link className="btn primary" href="/register">Register</Link></div></nav><section className="section"><div className="eyebrow">Academy</div><h1 style={{fontFamily:'Space Grotesk',fontSize:56,margin:'15px 0'}}>Private cybersecurity training.</h1><p className="muted" style={{maxWidth:700,lineHeight:1.8}}>Courses are protected by server-side enrollment. Register, then access is granted by the administrator.</p>{loading?<p className="muted">Loading courses…</p>:<div className="grid" style={{marginTop:35}}>{courses.map(c=><article className="card" key={c.id}><span className="tag">{c.category}</span><h3>{c.title}</h3><p>{c.description}</p><p className="small muted">{c.level} · {Math.round(c.duration_minutes/60*10)/10} hours</p><div className="actions"><Link className="btn" href={`/course/${c.slug}`}>{c.enrolled?'Open course':'View details'}</Link></div></article>)}</div>}</section></div></main>}