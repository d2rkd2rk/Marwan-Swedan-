'use client';
import {FormEvent,useState} from 'react';
import Link from 'next/link';
export function AuthForm({mode}:{mode:'login'|'register'}){const [data,setData]=useState<any>({});const [error,setError]=useState('');const [busy,setBusy]=useState(false);const [showPassword,setShowPassword]=useState(false);const [challengeId,setChallengeId]=useState('');const [otp,setOtp]=useState('');const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');const res=await fetch(`/api/auth/${mode}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(mode==='login'?{identifier:data.identifier,password:data.password}:data)});const json=await res.json();setBusy(false);if(!res.ok){setError(json.error||'Something went wrong.');return}if(mode==='login'&&json.requiresOtp){setChallengeId(json.challengeId);return}location.href=mode==='login'&&json.user?.role==='admin'?'/admin':'/courses'};const verify=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');const res=await fetch('/api/auth/verify-otp',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({challengeId,code:otp})});const json=await res.json();setBusy(false);if(!res.ok){setError(json.error||'Verification failed.');return}location.href=json.user?.role==='admin'?'/admin':'/courses'};return <div className="form"><Link className="brand" href="/">MARWAN.SWEDAN</Link>{challengeId?<><h1>Check your email.</h1><p className="muted">We sent a 6-digit verification code to your account email. It expires in 10 minutes.</p>{error&&<div className="error">{error}</div>}<form onSubmit={verify}><div className="field"><label>Verification code</label><input required inputMode="numeric" pattern="[0-9]{6}" maxLength={6} autoComplete="one-time-code" value={otp} onChange={e=>setOtp(e.target.value)}/></div><button className="btn primary" style={{width:'100%'}} disabled={busy}>{busy?'Verifying…':'Verify and sign in'}</button></form></>:<><h1>{mode==='login'?'Welcome back.':'Create your academy account.'}</h1><p className="muted">{mode==='login'?'Sign in to continue.':'Your account is private and course access is controlled by the administrator.'}</p>{error&&<div className="error">{error}</div>}<form onSubmit={submit}>{mode==='register'&&<><div className="field"><label>Full name</label><input required minLength={2} maxLength={80} onChange={e=>setData({...data,name:e.target.value})}/></div><div className="field"><label>Email</label><input required type="email" onChange={e=>setData({...data,email:e.target.value})}/></div><div className="field"><label>WhatsApp number</label><input required inputMode="tel" placeholder="2015…" onChange={e=>setData({...data,whatsapp:e.target.value})}/></div><div className="field"><label>Username</label><input required pattern="[A-Za-z0-9_]{3,24}" title="Use 3–24 letters, numbers, or underscores. No spaces." onChange={e=>setData({...data,username:e.target.value})}/><span className="small muted">3–24 letters, numbers, or underscores. No spaces.</span></div><div className="field"><label>Password</label><div style={{display:'flex',gap:8}}><input style={{flex:1}} required minLength={8} type={showPassword?'text':'password'} onChange={e=>setData({...data,password:e.target.value})}/><button className="btn" type="button" onClick={()=>setShowPassword(!showPassword)}>{showPassword?'Hide':'Show'}</button></div><span className="small muted">8+ characters, at least one number and one special character.</span></div></>}{mode==='login'&&<><div className="field"><label>Username or email</label><input required autoComplete="username" onChange={e=>setData({...data,identifier:e.target.value})}/></div><div className="field"><label>Password</label><div style={{display:'flex',gap:8}}><input style={{flex:1}} required autoComplete="current-password" type={showPassword?'text':'password'} onChange={e=>setData({...data,password:e.target.value})}/><button className="btn" type="button" onClick={()=>setShowPassword(!showPassword)}>{showPassword?'Hide':'Show'}</button></div></div></>}<button className="btn primary" style={{width:'100%',marginTop:10}} disabled={busy}>{busy?'Please wait…':mode==='login'?'Sign in':'Create account'}</button></form><p className="small muted">{mode==='login'?<>No account? <Link href="/register">Register</Link> · <Link href="/forgot-password">Forgot password?</Link></>:<>Already registered? <Link href="/login">Sign in</Link></>}</p><Link className="small muted" href="/">Back to portfolio</Link></>}</div>}