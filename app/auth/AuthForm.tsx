'use client';
import {FormEvent,useState} from 'react';
import Link from 'next/link';
export function AuthForm({mode}:{mode:'login'|'register'}){const [data,setData]=useState<any>({});const [error,setError]=useState('');const [busy,setBusy]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');const res=await fetch(`/api/auth/${mode}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(mode==='login'?{identifier:data.identifier,password:data.password}:data)});const json=await res.json();setBusy(false);if(!res.ok){setError(json.error||'Something went wrong.');return}location.href=mode==='login'&&json.user?.role==='admin'?'/admin':'/courses'};return <div className="form"><Link className="brand" href="/">MARWAN.SWEDAN</Link><h1>{mode==='login'?'Welcome back.':'Create your academy account.'}</h1><p className="muted">{mode==='login'?'Sign in to continue.':'Your account is private and course access is controlled by the administrator.'}</p>{error&&<div className="error">{error}</div>}<form onSubmit={submit}>{mode==='register'&&<><div className="field"><label>Full name</label><input required minLength={2} maxLength={80} onChange={e=>setData({...data,name:e.target.value})}/></div><div className="field"><label>Email</label><input required type="email" onChange={e=>setData({...data,email:e.target.value})}/></div><div className="field"><label>WhatsApp number</label><input required inputMode="tel" placeholder="2015…" onChange={e=>setData({...data,whatsapp:e.target.value})}/></div><div className="field"><label>Username</label><input required pattern="[A-Za-z0-9_]{3,24}" onChange={e=>setData({...data,username:e.target.value})}/></div></>}<div className="field"><label>{mode==='login'?'Username or email':'Password'}</label><input required type={mode==='login'?'text':'password'} onChange={e=>setData({...data,[mode==='login'?'identifier':'password']:e.target.value})}/></div>{mode==='register'&&<div className="field"><label>Password</label><input required minLength={8} type="password" onChange={e=>setData({...data,password:e.target.value})}/><span className="small muted">8+ characters, at least one number and one special character.</span></div>}<button className="btn primary" style={{width:'100%',marginTop:10}} disabled={busy}>{busy?'Please wait…':mode==='login'?'Sign in':'Create account'}</button></form><p className="small muted">{mode==='login'?<>No account? <Link href="/register">Register</Link></>:<>Already registered? <Link href="/login">Sign in</Link></>}</p><Link className="small muted" href="/">Back to portfolio</Link></div>}